// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  //API_URL : 'https://api.exchangeratesapi.io/latest?access_key=071ae2103f5fb8f8ca50d018f564a994'
  API_URL : 'http://api.exchangeratesapi.io/latest?access_key=071ae2103f5fb8f8ca50d018f564a994'
  //API_URL:'http://api.exchangeratesapi.io/latest?access_key=a5LRxbVxGIxT9Aa55ezAr0e79lF6N2sn'

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
